// Imports
// ========================================================
import { getDefaultConfig } from 'expo/metro-config';

// Exports
// ========================================================
export const defaultConfig = getDefaultConfig(__dirname);